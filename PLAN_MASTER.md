# IURALEX — PLAN MAESTRO DE IMPLEMENTACIÓN

> **Owner**: Nico (KAREN) · Generated by Claude Opus 2026-05-22 · Para ejecutar por Claude Sonnet
> **Project root**: `/Users/nicolasag/CRM-LEGAL`
> **App root**: `/Users/nicolasag/CRM-LEGAL/apps/web/src`
> **Brand color**: Cliender Purple `#8F7EE9` · Navy `#1E2839` · Black `#14181E` · Cream `#EBEAE4`
> **Stack**: Next.js 16 + TypeScript + Tailwind 4 + Supabase + Claude Haiku 4.5 (Anthropic API)
> **Demo mode**: `NEXT_PUBLIC_DEMO_MODE=1` activa lectura de `/lib/dev/demo-data.ts`
> **Container**: `iuralex` Docker · port 3002

---

## ÍNDICE

1. [Estado actual verificado](#1-estado-actual-verificado)
2. [Convenciones del código](#2-convenciones-del-código)
3. [SPRINT 1 — P0 BLOQUEADORES VENTA (5-6 días)](#3-sprint-1)
4. [SPRINT 2 — P1 IMPACTO DEMO (4-5 días)](#4-sprint-2)
5. [SPRINT 3 — P2 PROD HARDENING (5-7 días)](#5-sprint-3)
6. [SPRINT 4 — P3 ESCALA (4-6 semanas)](#6-sprint-4)
7. [Reglas de oro para ejecución](#7-reglas-de-oro)
8. [Glosario de archivos clave](#8-glosario)

---

## 1. ESTADO ACTUAL VERIFICADO

### Funcional 100%
- Auth NextAuth con JWT cookie `iuralex.session-token` · login `nicolas@cliender.com / Master123`
- Dashboard con KPIs (10 expedientes / 15 docs / 8 clientes / 6 contratos)
- Expedientes CRUD: list + detail + edit modal + summary modal (IA streaming)
- Clientes CRUD: list + detail + casos vinculados + chat LEXIA scoped
- Documentos: upload + auto-analyze (riesgo) + auto-classify (11 tipos) + ver análisis
- Acciones 80+ con modal central + form dinámico + LEXIA streaming
- Plantillas 20+ con creator wizard 3 pasos + edit/delete
- Plazos calculadora 41 plazos España (LEC/LECrim/LRJS/LPACAP/LJCA/LSC/LGT/CC/CP)
- Generador wizard 11 categorías × ~50 plantillas
- LEXIA chat con: typing dots, linkify BOE/CENDOJ, sin emojis/markdown bold, delete history
- Equipo: list + ficha trabajador con 4 tabs (Datos/Historial/Contratos/Permisos)
- Settings: 5 conectores reales (LexNET / Signaturit / vLex / AEAT / Google Workspace)
- Brand Cliender purple consistente
- API Anthropic LIVE (`ANTHROPIC_API_KEY` en `~/.env`)
- Migrations: 001 init · 002 RLS · 003 generated_contracts idempotente · 004 hardening (audit inmutable + indexes + CHECK constraints)

### Endpoints API existentes
```
/api/auth/login POST · /api/auth/logout POST · /api/auth/session GET · /api/auth/register POST
/api/cases GET POST · /api/cases/[caseId] GET PATCH DELETE · /api/cases/[caseId]/summary GET (SSE)
/api/clients GET POST · /api/clients/[id] GET PATCH DELETE
/api/documents GET POST · /api/documents/[docId] GET · /api/documents/[docId]/analysis GET
/api/documents/parse POST · /api/documents/classify POST
/api/claude/chat POST (SSE) · /api/claude/analyze-document POST · /api/claude/generate-contract POST
/api/claude/conversations GET DELETE · /api/claude/conversations/[convId] GET DELETE
/api/skills/execute POST (SSE) · /api/skills/list GET
/api/templates GET POST · /api/templates/[id] GET PUT DELETE
/api/lexia/sources GET
/api/admin/seed POST
/api/storage/auth/google/* (stub OAuth)
```

### GAPS críticos (= a construir)
- DocFlow (presupuestos / encargos / facturas) — **0%**
- Firma electrónica real (Signaturit OAuth + webhook) — **0%**
- Plazos persistentes activos por usuario + alertas cron — **0%**
- LEXIA agéntico (tool-use Claude para crear/editar entidades) — **0%**
- Grounding BOE real (Firecrawl over `site:boe.es`) — **0%**
- Search global Cmd+K cross-entity — palette existe básica, falta búsqueda real
- 2FA + SSO Google + rate limit Redis — **0%**
- Tests E2E automatizados — **0%**
- Email outbound (Resend/Postmark) — **0%**
- Cron jobs (Vercel scheduled / GH Actions cron) — **0%**
- Langfuse tracing activo en LEXIA — self-host levantado, no wire desde app
- Domain prod + TLS — solo localhost:3002
- Portal cliente externo (magic link) — **0%**

---

## 2. CONVENCIONES DEL CÓDIGO

### Patrón de endpoints
- Server actions o `route.ts` con `withErrorHandler` de `/lib/security/apiResponse.ts`
- Cada endpoint chequea `auth()` y throw `UnauthorizedError` si null
- Validación con Zod (importar desde `/lib/schemas.ts` o inline)
- Modo demo: detectar `IS_DEV_WITHOUT_DB` y leer `/lib/dev/demo-data.ts`
- Modo prod: `createServerClient` de `/lib/supabase/server.ts`

### Patrón de pages
- `'use client'` solo si necesita interactividad/estado
- `import { motion, AnimatePresence } from 'framer-motion'` ya disponible
- `import { ArrowLeft, ... } from 'lucide-react'` (named imports)
- Tipografía: h1 `text-4xl font-serif font-semibold` · h2 `text-2xl font-sans font-semibold` · body `text-sm leading-relaxed`
- CTA primario: `bg-[#8F7EE9] hover:bg-[#7C6BD6] text-white rounded-lg px-4 py-2 text-sm font-medium`
- CTA secundario: `bg-white border border-[#8F7EE9]/40 text-[#5B4FB8] hover:bg-[#8F7EE9]/5`
- Cards: `rounded-2xl border border-slate-200 bg-white hover:border-[#8F7EE9]/30 hover:shadow-md transition-all`
- Modales: backdrop `bg-slate-900/50 backdrop-blur-sm` · panel `rounded-2xl shadow-2xl max-w-3xl`
- NO usar `bg-violet-600`/`indigo-*`/`purple-*` — siempre `#8F7EE9`

### LEXIA system prompt
- Vive en `/lib/claude/prompts.ts` como `SYSTEM_PROMPT_LEGAL_CHAT`
- **NO modificar reglas 1-9** (precision, jurispr, plazos, foral, cierre, no-aluc, BOE links, no-markdown)
- Estilo: profesional cercano, conciso, sin emojis, sin `**bold**`, citas con URL BOE entre paréntesis

### Demo data
- `/lib/dev/demo-data.ts` — siempre añadir nuevas entidades aquí con datos sintéticos realistas españoles
- Mantener IDs UUID v4 patterns: `c1000000-...-001` clientes · `e1000000-...-001` casos · `d1000000-...-001` docs
- Helper `daysAgo(n)` para fechas relativas

### Build mode
- Container `iuralex` corre `npm run dev` (Turbopack)
- Cache `/app/.next` en volumen Docker (rápido)
- HMR funciona — guardar archivo recompila page específica
- Si build production deseado: cambiar `docker-compose.yml:25` command a `npm run build && npm run start` (1-2min build inicial)

---

## 3. SPRINT 1 — P0 BLOQUEADORES VENTA

**Objetivo**: cerrar el 80% de objeciones de despachos serios. Sin esto no se vende.
**Total**: ~6 días.

### TASK 1.1 — DocFlow MVP (5 días)

**Problema**: hoy IURALEX no genera presupuesto/hoja encargo/factura. Es el gap #1 declarado por prospects (Luis Roca explícito).

#### 1.1.1 Schema DB — `/supabase/migrations/005_docflow.sql`

```sql
CREATE TABLE IF NOT EXISTS fee_schedules (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  case_area TEXT NOT NULL,
  case_type TEXT NOT NULL,
  label TEXT NOT NULL,
  base_amount_cents INT NOT NULL DEFAULT 0,
  hourly_rate_cents INT DEFAULT 0,
  success_fee_pct NUMERIC(5,2) DEFAULT 0,
  iva_pct NUMERIC(4,2) DEFAULT 21.00,
  active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
ALTER TABLE fee_schedules ENABLE ROW LEVEL SECURITY;
CREATE POLICY "fees_manage_own" ON fee_schedules
  FOR ALL USING ((SELECT auth.uid()) = user_id) WITH CHECK ((SELECT auth.uid()) = user_id);
CREATE INDEX idx_fees_user_active ON fee_schedules(user_id) WHERE active = TRUE;

CREATE TABLE IF NOT EXISTS budgets (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  case_id UUID REFERENCES cases(id) ON DELETE CASCADE,
  client_id UUID REFERENCES clients(id) ON DELETE SET NULL,
  budget_number TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'draft',
  total_cents INT NOT NULL,
  iva_cents INT NOT NULL,
  grand_total_cents INT NOT NULL,
  line_items JSONB NOT NULL DEFAULT '[]',
  pdf_storage_path TEXT,
  sent_at TIMESTAMPTZ,
  accepted_at TIMESTAMPTZ,
  expires_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
ALTER TABLE budgets ENABLE ROW LEVEL SECURITY;
CREATE POLICY "budgets_manage_own" ON budgets
  FOR ALL USING ((SELECT auth.uid()) = user_id) WITH CHECK ((SELECT auth.uid()) = user_id);
CREATE INDEX idx_budgets_user_case ON budgets(user_id, case_id);
CREATE INDEX idx_budgets_status ON budgets(status);

CREATE TABLE IF NOT EXISTS engagements (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  case_id UUID REFERENCES cases(id) ON DELETE CASCADE,
  client_id UUID NOT NULL REFERENCES clients(id) ON DELETE CASCADE,
  budget_id UUID REFERENCES budgets(id) ON DELETE SET NULL,
  status TEXT NOT NULL DEFAULT 'pending_sign',
  content_markdown TEXT NOT NULL,
  pdf_storage_path TEXT,
  signaturit_request_id TEXT,
  sign_url TEXT,
  signed_at TIMESTAMPTZ,
  signed_ip TEXT,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
ALTER TABLE engagements ENABLE ROW LEVEL SECURITY;
CREATE POLICY "engagements_manage_own" ON engagements
  FOR ALL USING ((SELECT auth.uid()) = user_id) WITH CHECK ((SELECT auth.uid()) = user_id);
CREATE INDEX idx_engagements_user_case ON engagements(user_id, case_id);

CREATE TABLE IF NOT EXISTS invoices (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  case_id UUID REFERENCES cases(id) ON DELETE SET NULL,
  client_id UUID NOT NULL REFERENCES clients(id) ON DELETE RESTRICT,
  invoice_number TEXT NOT NULL,
  series TEXT NOT NULL DEFAULT 'F',
  status TEXT NOT NULL DEFAULT 'draft',
  issue_date DATE NOT NULL DEFAULT CURRENT_DATE,
  due_date DATE,
  paid_date DATE,
  base_cents INT NOT NULL,
  iva_cents INT NOT NULL,
  irpf_cents INT DEFAULT 0,
  total_cents INT NOT NULL,
  line_items JSONB NOT NULL DEFAULT '[]',
  notes TEXT,
  pdf_storage_path TEXT,
  facturae_xml_path TEXT,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE (user_id, invoice_number)
);
ALTER TABLE invoices ENABLE ROW LEVEL SECURITY;
CREATE POLICY "invoices_manage_own" ON invoices
  FOR ALL USING ((SELECT auth.uid()) = user_id) WITH CHECK ((SELECT auth.uid()) = user_id);
CREATE INDEX idx_invoices_user_case ON invoices(user_id, case_id);
CREATE INDEX idx_invoices_status ON invoices(user_id, status);
CREATE INDEX idx_invoices_client ON invoices(client_id);

CREATE TABLE IF NOT EXISTS invoice_sequences (
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  series TEXT NOT NULL,
  year INT NOT NULL,
  next_number INT NOT NULL DEFAULT 1,
  PRIMARY KEY (user_id, series, year)
);
ALTER TABLE invoice_sequences ENABLE ROW LEVEL SECURITY;
CREATE POLICY "seq_manage_own" ON invoice_sequences
  FOR ALL USING ((SELECT auth.uid()) = user_id);

CREATE OR REPLACE FUNCTION next_invoice_number(p_user_id UUID, p_series TEXT, p_year INT)
RETURNS INT AS $$
DECLARE n INT;
BEGIN
  INSERT INTO invoice_sequences (user_id, series, year, next_number)
  VALUES (p_user_id, p_series, p_year, 2)
  ON CONFLICT (user_id, series, year) DO UPDATE
    SET next_number = invoice_sequences.next_number + 1
    RETURNING next_number - 1 INTO n;
  IF n IS NULL THEN
    SELECT next_number - 1 INTO n FROM invoice_sequences WHERE user_id=p_user_id AND series=p_series AND year=p_year;
  END IF;
  RETURN n;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

#### 1.1.2 Demo data — añadir a `/lib/dev/demo-data.ts`

```ts
export interface DemoFeeSchedule {
  id: string; user_id: string; case_area: string; case_type: string
  label: string; base_amount_cents: number; iva_pct: number; active: boolean
}

export const DEMO_FEE_SCHEDULES: DemoFeeSchedule[] = [
  { id: 'fs-001', user_id: DEMO_USER_ID, case_area: 'Civil', case_type: 'monitorio', label: 'Monitorio < 30.000€', base_amount_cents: 80000, iva_pct: 21, active: true },
  { id: 'fs-002', user_id: DEMO_USER_ID, case_area: 'Civil', case_type: 'reclamacion_cantidad', label: 'Reclamación cantidad ordinario', base_amount_cents: 250000, iva_pct: 21, active: true },
  { id: 'fs-003', user_id: DEMO_USER_ID, case_area: 'Laboral', case_type: 'despido_improc', label: 'Despido improcedente', base_amount_cents: 120000, iva_pct: 21, active: true },
  { id: 'fs-004', user_id: DEMO_USER_ID, case_area: 'Familia', case_type: 'divorcio_cont', label: 'Divorcio contencioso', base_amount_cents: 200000, iva_pct: 21, active: true },
  { id: 'fs-005', user_id: DEMO_USER_ID, case_area: 'Mercantil', case_type: 'constitucion_sl', label: 'Constitución SL completa', base_amount_cents: 90000, iva_pct: 21, active: true },
  // +10 más cubriendo todas las áreas
]

export const DEMO_BUDGETS = [/* 4 budgets: 1 draft, 2 sent, 1 accepted */]
export const DEMO_ENGAGEMENTS = [/* 2-3 entries: 1 pending_sign, 1 signed */]
export const DEMO_INVOICES = [/* 5 invoices: 1 draft, 2 issued, 1 paid, 1 overdue */]
```

#### 1.1.3 Endpoints a crear

```
/api/fee-schedules/route.ts          GET POST
/api/fee-schedules/[id]/route.ts     GET PUT DELETE
/api/budgets/route.ts                GET POST
/api/budgets/[id]/route.ts           GET PATCH DELETE
/api/budgets/[id]/send/route.ts      POST
/api/budgets/[id]/accept/route.ts    POST (magic link público)
/api/budgets/[id]/pdf/route.ts       GET
/api/engagements/route.ts            GET POST
/api/engagements/[id]/route.ts       GET PATCH
/api/engagements/[id]/sign/route.ts  POST
/api/engagements/[id]/webhook/route.ts POST (Signaturit callback)
/api/invoices/route.ts               GET POST
/api/invoices/[id]/route.ts          GET PATCH DELETE
/api/invoices/[id]/issue/route.ts    POST
/api/invoices/[id]/pay/route.ts      POST
/api/invoices/[id]/pdf/route.ts      GET
```

Pattern invoice_number atómico:
```ts
async function nextInvoiceNumber(supabase, userId, series='F') {
  const year = new Date().getFullYear()
  const { data } = await supabase.rpc('next_invoice_number',
    { p_user_id: userId, p_series: series, p_year: year })
  return `${series}-${year}-${String(data).padStart(4, '0')}`
}
```

#### 1.1.4 PDF generation

Install:
```bash
cd /Users/nicolasag/CRM-LEGAL/apps/web && npm i @react-pdf/renderer --legacy-peer-deps
```

Files:
- `/lib/pdf/BudgetPDF.tsx` — React-PDF component (Page, Document, Text, View, StyleSheet)
- `/lib/pdf/EngagementPDF.tsx`
- `/lib/pdf/InvoicePDF.tsx`
- `/lib/pdf/render.ts` — `renderToBuffer(component): Promise<Buffer>`

Cada PDF: header con logo Cliender + nº doc + fecha · datos despacho (settings) · datos cliente · tabla line_items · totales · footer condiciones + firma electrónica disclaimer.

#### 1.1.5 UI Components

```
/app/(dashboard)/finanzas/page.tsx                  — landing tabs Presupuestos / Encargos / Facturas
/app/(dashboard)/finanzas/budgets/page.tsx
/app/(dashboard)/finanzas/budgets/[id]/page.tsx
/app/(dashboard)/finanzas/invoices/page.tsx
/app/(dashboard)/finanzas/invoices/[id]/page.tsx
/app/(dashboard)/finanzas/honorarios/page.tsx       — tarifas configurables
/components/features/docflow/BudgetForm.tsx
/components/features/docflow/InvoiceForm.tsx
/components/features/docflow/LineItemsEditor.tsx
/components/features/docflow/StatusBadge.tsx
```

Sidebar nav `/app/(dashboard)/layout.tsx`: añadir `{ href: '/finanzas', label: 'Finanzas', icon: 'euro' }` después de `/plazos`.

Case detail `/cases/[caseId]/page.tsx`: nuevo botón "Generar presupuesto" junto a "Resumir con IA". Click abre modal con form pre-rellenado desde `fee_schedules[case_area + case_type]`. Submit → POST `/api/budgets` → redirect `/finanzas/budgets/[id]`.

#### 1.1.6 Auto-flow case → invoice

En PATCH `/api/cases/[caseId]` cuando `status` cambia a `closed`:
```ts
if (oldStatus !== 'closed' && newStatus === 'closed') {
  await maybeAutoGenerateInvoice(caseId, userId, supabase)
}
```

`maybeAutoGenerateInvoice` busca engagement signed → crea invoice draft con line_items del budget aceptado → email "Caso cerrado. Factura draft generada. Revísala."

#### 1.1.7 Acceptance criteria

- [ ] Migration 005 corre idempotente (aplicar 2 veces sin errores)
- [ ] `/finanzas` carga con tabs y stats reales
- [ ] Creo budget desde case detail → aparece en `/finanzas/budgets`
- [ ] Click "Generar PDF" → descarga PDF con logo Cliender + datos demo coherentes
- [ ] Marco invoice `issued` → recibe número correlativo F-2026-XXXX único
- [ ] Cierro un caso → invoice draft generada automáticamente
- [ ] Demo data: 4 budgets / 3 engagements / 5 invoices en estados mixtos
- [ ] LEXIA puede mencionar el presupuesto cuando pregunta por el cliente

**Estimación**: 5 días (1.5d schema+endpoints · 1d PDF · 1.5d UI · 1d auto-flow + polish)

---

### TASK 1.2 — Cron alertas plazos (1.5 días)

**Problema**: calculadora de plazos manual. Nadie avisa al abogado 5 días antes de vencer.

#### 1.2.1 Schema — migration 006

```sql
CREATE TABLE IF NOT EXISTS case_deadlines (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  case_id UUID NOT NULL REFERENCES cases(id) ON DELETE CASCADE,
  client_id UUID REFERENCES clients(id),
  action TEXT NOT NULL,
  law_ref TEXT,
  category TEXT NOT NULL,
  deadline_date TIMESTAMPTZ NOT NULL,
  priority TEXT NOT NULL DEFAULT 'medium',
  status TEXT NOT NULL DEFAULT 'pending',
  notify_15d BOOLEAN DEFAULT TRUE,
  notify_5d BOOLEAN DEFAULT TRUE,
  notify_1d BOOLEAN DEFAULT TRUE,
  notified_15d_at TIMESTAMPTZ,
  notified_5d_at TIMESTAMPTZ,
  notified_1d_at TIMESTAMPTZ,
  completed_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
ALTER TABLE case_deadlines ENABLE ROW LEVEL SECURITY;
CREATE POLICY "deadlines_manage_own" ON case_deadlines
  FOR ALL USING ((SELECT auth.uid()) = user_id) WITH CHECK ((SELECT auth.uid()) = user_id);
CREATE INDEX idx_deadlines_user_status_date ON case_deadlines(user_id, status, deadline_date);
CREATE INDEX idx_deadlines_notify ON case_deadlines(deadline_date) WHERE status='pending';
```

#### 1.2.2 Demo data

`DEMO_DEADLINES`: 12 entries (3 expired / 4 urgent / 3 próximos / 2 futuros) vinculados a DEMO_CASES.

#### 1.2.3 Endpoints

```
/api/deadlines/route.ts            GET (filtros ?status, ?category, ?days_remaining) POST
/api/deadlines/[id]/route.ts       GET PATCH DELETE
/api/deadlines/[id]/complete/route.ts POST
/api/deadlines/cron/route.ts       GET (protected via CRON_SECRET)
```

Cron protection:
```ts
export async function GET(req: NextRequest) {
  const auth = req.headers.get('authorization')
  if (auth !== `Bearer ${process.env.CRON_SECRET}`) return new Response('Unauthorized', { status: 401 })
  // escanea deadlines pending, calcula días restantes, envía email por threshold
}
```

#### 1.2.4 Cron scheduler — GitHub Actions

`/.github/workflows/deadline-cron.yml`:
```yaml
name: Deadline Cron
on:
  schedule:
    - cron: '0 8 * * 1-5'
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - run: curl -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" https://iuralex.cliender.com/api/deadlines/cron
```

#### 1.2.5 UI

Dashboard widget en `/app/(dashboard)/dashboard/page.tsx`:
- Nueva card "Plazos activos" tras "Acciones rápidas"
- 3 contadores badge color: vencidos rojo / urgentes ámbar / próximos amarillo
- Top 5 más urgentes con action, case, fecha, días restantes, "Marcar completado"
- Link "Ver todos" → `/plazos?view=active`

Banner sticky `/app/(dashboard)/layout.tsx`:
- Si `expired > 0 || urgent > 0` y no dismissed sesión → banner rojo fino con CTA "Ver plazos"
- Dismissible (sessionStorage)

Plazos page `/app/(dashboard)/plazos/page.tsx`:
- Nueva tab "Mis plazos activos" arriba (default si `?view=active`)
- Tabs internas: Vencidos / Urgentes / Próximos / Todos / Completados
- Row: action, case + link, client, deadline_date, days remaining, badge prioridad, acciones

#### 1.2.6 Email notification

`/lib/emails/deadline-alert.tsx`:
```
Hola Nicolas,
Recordatorio: tienes un plazo crítico en [X] días.
Acción: Contestación demanda
Caso: Reclamación vicios constructivos — Promoción Vallecas
Cliente: Construcciones Mediterráneo S.L.
Fecha límite: 28 de mayo de 2026
Base normativa: LEC art. 404
Ver expediente → https://iuralex.cliender.com/cases/...
```

#### 1.2.7 Acceptance criteria

- [ ] Tabla `case_deadlines` creada con RLS
- [ ] Demo data 12 entries
- [ ] GET `/api/deadlines?status=pending` devuelve lista filtrada
- [ ] Crear deadline desde case detail (nuevo botón "+ Plazo")
- [ ] Dashboard widget muestra contadores correctos
- [ ] Banner top aparece si vencidos/urgentes
- [ ] Endpoint cron protegido con CRON_SECRET
- [ ] Llamada manual al cron envía email correcto

**Estimación**: 1.5 días

---

### TASK 1.3 — Signaturit integración real (1 día)

#### 1.3.1 Schema — migration 007

```sql
CREATE TABLE IF NOT EXISTS integration_credentials (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  provider TEXT NOT NULL,
  access_token_enc TEXT NOT NULL,
  refresh_token_enc TEXT,
  scope TEXT,
  expires_at TIMESTAMPTZ,
  meta JSONB DEFAULT '{}',
  created_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE (user_id, provider)
);
ALTER TABLE integration_credentials ENABLE ROW LEVEL SECURITY;
CREATE POLICY "creds_manage_own" ON integration_credentials
  FOR ALL USING ((SELECT auth.uid()) = user_id);
```

#### 1.3.2 Crypto lib — `/lib/security/crypto.ts`

```ts
import crypto from 'crypto'
const ALG = 'aes-256-gcm'
const KEY = Buffer.from(process.env.ENCRYPTION_KEY!, 'hex')

export function encrypt(plaintext: string): string {
  const iv = crypto.randomBytes(12)
  const cipher = crypto.createCipheriv(ALG, KEY, iv)
  const enc = Buffer.concat([cipher.update(plaintext, 'utf8'), cipher.final()])
  const tag = cipher.getAuthTag()
  return [iv.toString('base64'), tag.toString('base64'), enc.toString('base64')].join(':')
}

export function decrypt(token: string): string {
  const [iv, tag, enc] = token.split(':').map(s => Buffer.from(s, 'base64'))
  const decipher = crypto.createDecipheriv(ALG, KEY, iv)
  decipher.setAuthTag(tag)
  return Buffer.concat([decipher.update(enc), decipher.final()]).toString('utf8')
}
```

Generar `ENCRYPTION_KEY`: `openssl rand -hex 32` → guardar en `~/.env`.

#### 1.3.3 Signaturit endpoints

```
/api/integrations/signaturit/connect/route.ts    GET (redirect OAuth)
/api/integrations/signaturit/callback/route.ts   GET (recibe code, guarda token cifrado)
/api/integrations/signaturit/disconnect/route.ts POST
/api/engagements/[id]/sign/route.ts              POST
/api/engagements/[id]/webhook/route.ts           POST (callback)
```

Webhook security: Signaturit firma con HMAC-SHA256 + `SIGNATURIT_WEBHOOK_SECRET`. Verificar header `X-Signature`.

#### 1.3.4 UI

Settings tab Conexiones: Signaturit card botón "Conectar" → `/api/integrations/signaturit/connect`. Si conectado: estado verde + "Desconectar".

Engagement detail (nueva `/finanzas/engagements/[id]/page.tsx`):
- Si `status === 'pending_sign'` → botón "Enviar a firmar"
- Estado live: pending_sign / signed con timestamp + IP

#### 1.3.5 Acceptance criteria

- [ ] OAuth flow Signaturit completo (sandbox)
- [ ] Token cifrado en `integration_credentials`
- [ ] `ENCRYPTION_KEY` documentada en `.env.example`
- [ ] Crear engagement → "Enviar a firmar" → cliente firma → webhook actualiza status
- [ ] LEXIA puede consultar "¿está firmada la hoja de encargo de [cliente]?"

**Estimación**: 1 día

---

## 4. SPRINT 2 — P1 IMPACTO DEMO

**Objetivo**: wow moments en demos. Diferenciación clara vs vLex/Aranzadi.
**Total**: ~5 días.

### TASK 2.1 — LEXIA agéntico con tool-use (3 días)

**Problema**: LEXIA hoy solo "responde". Queremos que ejecute: crear caso, abrir doc, buscar jurisprudencia, programar plazo, enviar email.

#### 2.1.1 Tool definitions — `/lib/lexia/tools.ts`

```ts
export const LEXIA_TOOLS = [
  {
    name: 'create_case',
    description: 'Crea un nuevo expediente en el sistema. Usar cuando el abogado pida abrir un caso nuevo.',
    input_schema: {
      type: 'object',
      properties: {
        title: { type: 'string' },
        description: { type: 'string' },
        area: { type: 'string', enum: ['Civil','Penal','Laboral','Mercantil','Familia','Administrativo','Fiscal'] },
        client_id: { type: 'string' },
        amount_cents: { type: 'integer' },
      },
      required: ['title', 'area'],
    },
  },
  {
    name: 'search_jurisprudence',
    description: 'Busca jurisprudencia española en BOE/CENDOJ. Hasta 5 citas con URL oficial.',
    input_schema: {
      type: 'object',
      properties: {
        query: { type: 'string' },
        source: { type: 'string', enum: ['boe','cendoj','tc','aepd','auto'], default: 'auto' },
        limit: { type: 'integer', default: 5 },
      },
      required: ['query'],
    },
  },
  {
    name: 'schedule_deadline',
    description: 'Programa un plazo procesal vinculado a un caso. Calcula fecha límite automáticamente.',
    input_schema: {
      type: 'object',
      properties: {
        case_id: { type: 'string' },
        action: { type: 'string' },
        law_ref: { type: 'string' },
        start_date: { type: 'string', format: 'date' },
        days: { type: 'integer' },
        unit: { type: 'string', enum: ['business','calendar','months'], default: 'business' },
        priority: { type: 'string', enum: ['critical','high','medium','low'], default: 'high' },
      },
      required: ['case_id', 'action', 'start_date', 'days'],
    },
  },
  {
    name: 'generate_document',
    description: 'Genera un documento jurídico usando una plantilla.',
    input_schema: {
      type: 'object',
      properties: {
        template_id: { type: 'string' },
        field_values: { type: 'object' },
        case_id: { type: 'string' },
      },
      required: ['template_id', 'field_values'],
    },
  },
  {
    name: 'find_client',
    description: 'Busca cliente por nombre o NIF.',
    input_schema: { type: 'object', properties: { query: { type: 'string' } }, required: ['query'] },
  },
  {
    name: 'get_case_summary',
    description: 'Devuelve resumen del expediente y documentos vinculados.',
    input_schema: { type: 'object', properties: { case_id: { type: 'string' } }, required: ['case_id'] },
  },
  {
    name: 'send_email',
    description: 'Envía email al cliente. Requiere confirmación humana antes de ejecutar.',
    input_schema: {
      type: 'object',
      properties: {
        client_id: { type: 'string' },
        subject: { type: 'string' },
        body: { type: 'string' },
      },
      required: ['client_id', 'subject', 'body'],
    },
  },
] as const
```

#### 2.1.2 Tool executors — `/lib/lexia/tool-executors.ts`

```ts
type ToolResult = { ok: boolean; data?: any; error?: string }

export async function executeToolCall(
  toolName: string,
  input: Record<string, unknown>,
  ctx: { userId: string; supabase: any; demo: boolean }
): Promise<ToolResult> {
  switch (toolName) {
    case 'create_case':          return createCaseImpl(input, ctx)
    case 'search_jurisprudence': return searchJurisprudenceImpl(input, ctx)
    case 'schedule_deadline':    return scheduleDeadlineImpl(input, ctx)
    case 'generate_document':    return generateDocumentImpl(input, ctx)
    case 'find_client':          return findClientImpl(input, ctx)
    case 'get_case_summary':     return getCaseSummaryImpl(input, ctx)
    case 'send_email':           return { ok: false, error: 'Requiere confirmación humana — usar UI' }
    default:                     return { ok: false, error: `Tool desconocida: ${toolName}` }
  }
}
```

Cada `*Impl` ejecuta contra Supabase (prod) o demo-data (demo mode). Respetar RLS (filtrar por `ctx.userId`).

#### 2.1.3 Agentic loop — modificar `/api/claude/chat/route.ts`

```ts
import { LEXIA_TOOLS } from '@/lib/lexia/tools'
import { executeToolCall } from '@/lib/lexia/tool-executors'

const MAX_ITERATIONS = 5

async function agenticLoop(messages, ctx, send) {
  let iteration = 0
  while (iteration < MAX_ITERATIONS) {
    iteration++
    const response = await client.messages.create({
      model: MODEL,
      max_tokens: 4000,
      system: SYSTEM_PROMPT_LEGAL_CHAT,
      tools: LEXIA_TOOLS,
      messages,
    })

    for (const block of response.content) {
      if (block.type === 'text') {
        send({ type: 'delta', text: block.text })
      } else if (block.type === 'tool_use') {
        send({ type: 'tool_start', name: block.name, input: block.input })
        const result = await executeToolCall(block.name, block.input, ctx)
        send({ type: 'tool_result', name: block.name, ok: result.ok, data: result.data })
        messages.push({ role: 'assistant', content: response.content })
        messages.push({
          role: 'user',
          content: [{ type: 'tool_result', tool_use_id: block.id, content: JSON.stringify(result) }],
        })
      }
    }

    if (response.stop_reason !== 'tool_use') break
  }
}
```

Nota: la versión actual usa `messages.stream()`. Para tool-use bloqueante con emit por bloque, o stream con buffering de `content_block_*` events. Decisión simple primero: bloqueante.

#### 2.1.4 UI tool feedback

En `/app/(dashboard)/chat/page.tsx` handler SSE:

```ts
if (evt.type === 'tool_start') {
  setMessages(prev => [...prev, { role: 'tool', tool: evt.name, status: 'running', input: evt.input }])
} else if (evt.type === 'tool_result') {
  setMessages(prev => prev.map(m =>
    m.tool === evt.name && m.status === 'running'
      ? { ...m, status: evt.ok ? 'done' : 'error', data: evt.data }
      : m
  ))
}
```

Render tool messages como cards inline (Cursor-style):
```
🛠 Buscando jurisprudencia...
   query: "despido improcedente plazos"
   ✓ 3 resultados encontrados
   • STS 735/2025 (https://...)
   • Ley 1/2000 LEC...
```

#### 2.1.5 Acceptance criteria

- [ ] "Abre un nuevo caso laboral por despido de María García" → crea expediente real
- [ ] "Programa plazo de contestación demanda para caso X notificada hoy" → crea deadline con fecha calculada
- [ ] "Búscame jurisprudencia sobre despido improcedente" → llama search_jurisprudence → 3-5 citas
- [ ] UI muestra cada tool call con estado running/done/error
- [ ] Tools respetan RLS (no tocan datos de otros user_id)
- [ ] Tool `send_email` siempre pide confirmación antes

**Estimación**: 3 días

---

### TASK 2.2 — CaseHunter agente (2 días)

#### 2.2.1 Schema — migration 008

```sql
CREATE TABLE IF NOT EXISTS client_signals (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  client_id UUID NOT NULL REFERENCES clients(id) ON DELETE CASCADE,
  signal_type TEXT NOT NULL,
  score NUMERIC(4,2) NOT NULL DEFAULT 0,
  reason TEXT NOT NULL,
  evidence JSONB DEFAULT '{}',
  status TEXT NOT NULL DEFAULT 'pending',
  detected_at TIMESTAMPTZ DEFAULT now(),
  actioned_at TIMESTAMPTZ,
  actioned_by UUID REFERENCES users(id)
);
ALTER TABLE client_signals ENABLE ROW LEVEL SECURITY;
CREATE POLICY "signals_own" ON client_signals FOR ALL USING ((SELECT auth.uid()) = user_id);
CREATE INDEX idx_signals_user_status_score ON client_signals(user_id, status, score DESC);
```

#### 2.2.2 Endpoint cron — `/api/casehunter/cron/route.ts`

Para cada user activo:
1. Clientes sin caso activo > 18 meses → score reactivation
2. Clientes empresa con casos laborales pasados → score iguala_candidate
3. Clientes individuales > 60 años con casos civiles → score herencia_probable

Llama Claude Haiku con context cliente para generar `reason`. INSERT en `client_signals`. Mensual via GH Actions.

#### 2.2.3 UI — `/app/(dashboard)/insights/page.tsx`

- Hero "Oportunidades detectadas por CaseHunter"
- Tabs: Reactivaciones / Igualas potenciales / Herencias / Seguimientos
- Cada signal: cliente, score badge, reason, evidence expandible, "Llamar" / "Marcar accionado" / "Descartar"
- Filtros score min, fecha
- Sidebar nav: `{ href: '/insights', label: 'Insights', icon: 'sparkles', badge: 'IA' }`

#### 2.2.4 Demo data

15-20 `DEMO_CLIENT_SIGNALS` con razones realistas vinculados a DEMO_CLIENTS.

#### 2.2.5 Acceptance criteria

- [ ] Endpoint cron genera signals coherentes
- [ ] `/insights` muestra lista priorizada por score
- [ ] Marcar actioned cambia estado
- [ ] Demo data poblado realista

**Estimación**: 2 días

---

### TASK 2.3 — Grounding BOE real con Firecrawl (2 días)

#### 2.3.1 Firecrawl integration

```bash
npm i @mendable/firecrawl-js
```

Extender `/lib/lexia/grounding.ts`:

```ts
export async function searchSpanishSourcesLive(query: string): Promise<LegalCitation[]> {
  if (process.env.NEXT_PUBLIC_DEMO_MODE === '1' || !process.env.FIRECRAWL_API_KEY) {
    return searchSpanishSources(query)
  }

  const fc = new FirecrawlApp({ apiKey: process.env.FIRECRAWL_API_KEY })
  const sites = ['site:boe.es', 'site:poderjudicial.es', 'site:aepd.es']

  const results = await Promise.all(sites.map(site =>
    fc.search(`${site} ${query}`, { limit: 3, scrapeOptions: { formats: ['markdown'] } })
  ))

  const citations: LegalCitation[] = []
  // mapping logic: extraer title, url, snippet, fecha de cada result
  return citations.slice(0, 5)
}
```

#### 2.3.2 Wire en tool executor

En `searchJurisprudenceImpl` (TASK 2.1.2): usar `searchSpanishSourcesLive` en lugar del mock.

#### 2.3.3 Cache 24h — migration 008b

```sql
CREATE TABLE jurisprudence_cache (
  query_hash TEXT PRIMARY KEY,
  results JSONB NOT NULL,
  cached_at TIMESTAMPTZ DEFAULT now(),
  expires_at TIMESTAMPTZ NOT NULL
);
```

#### 2.3.4 Acceptance criteria

- [ ] Tool search_jurisprudence ejecuta Firecrawl real
- [ ] Resultados incluyen URL oficial verificable + snippet + fecha
- [ ] Cache 24h reduce llamadas duplicadas
- [ ] Demo mode sigue devolviendo mocks (no consume Firecrawl quota)

**Estimación**: 2 días

---

### TASK 2.4 — Search global Cmd+K cross-entity (1 día)

#### 2.4.1 Mejorar `/components/features/command-palette/index.tsx`

- Debounce 200ms input
- Fetch parallel: `/api/cases`, `/api/clients`, `/api/documents` (usar `useCachedFetch`)
- Filtrar client-side por title/name/case_number/nif_cif/email
- Agrupar resultados con headers: Expedientes / Clientes / Documentos / Acciones del sistema
- Highlight query en match
- Limit 5 por categoría
- Keyboard nav: arrow up/down + Enter + ESC
- Si query > 3 chars y 0 resultados → última opción "Preguntar a LEXIA: <query>" → navega `/chat?q=<query>`

#### 2.4.2 Acceptance criteria

- [ ] Cmd+K abre palette
- [ ] "García" → encuentra María García Pérez + casos donde aparezca
- [ ] "Vallecas" → encuentra expediente + docs vinculados
- [ ] Arrow keys + Enter funcionan
- [ ] Fallback "Preguntar a LEXIA" aparece si 0 matches

**Estimación**: 1 día

---

## 5. SPRINT 3 — P2 PROD HARDENING

**Objetivo**: prod-ready para tráfico real.
**Total**: ~7 días.

### TASK 3.1 — Auth hardening (3 días)

#### 3.1.1 2FA TOTP — migration 009

```sql
ALTER TABLE users ADD COLUMN totp_secret_enc TEXT;
ALTER TABLE users ADD COLUMN totp_enabled BOOLEAN DEFAULT FALSE;
ALTER TABLE users ADD COLUMN totp_backup_codes_enc TEXT[];
```

Lib: `npm i otplib qrcode`

Endpoints:
- `/api/auth/2fa/setup POST` → genera secret + qrcode_data_url
- `/api/auth/2fa/verify POST` → valida código, activa
- `/api/auth/2fa/disable POST` → requiere password + código

Login flow: tras password OK, si `totp_enabled` → segunda pantalla pide código.

#### 3.1.2 Google SSO

NextAuth provider Google en `/lib/auth/auth.config.ts`:
```ts
import GoogleProvider from 'next-auth/providers/google'
providers: [
  CredentialsProvider({...}),
  GoogleProvider({ clientId: env.GOOGLE_CLIENT_ID, clientSecret: env.GOOGLE_CLIENT_SECRET, ... }),
]
```

Crear app Google Cloud Console + redirect URI `https://iuralex.cliender.com/api/auth/callback/google`.

UI login page: botón "Continuar con Google" arriba.

#### 3.1.3 Rate limit Redis

Reemplazar `Map<string, ...>` in-memory de `middleware.ts:14`:

```ts
import { Ratelimit } from '@upstash/ratelimit'
import { Redis } from '@upstash/redis'

const redis = Redis.fromEnv()
const ratelimit = new Ratelimit({
  redis,
  limiter: Ratelimit.slidingWindow(100, '1 m'),
  analytics: true,
})

const { success } = await ratelimit.limit(ip)
if (!success) return new Response('Too many requests', { status: 429 })
```

Vars env: `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN` (Upstash free tier).

#### 3.1.4 Reset password — migration 010

```sql
CREATE TABLE password_resets (
  token TEXT PRIMARY KEY,
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  expires_at TIMESTAMPTZ NOT NULL,
  used_at TIMESTAMPTZ
);
```

Endpoints:
- `/api/auth/forgot-password POST` → email con token
- `/api/auth/reset-password POST` → cambia password

Pages: `/forgot-password` y `/reset-password/[token]`.

#### 3.1.5 Acceptance criteria

- [ ] User activa 2FA → próximo login pide código
- [ ] Login con Google funciona
- [ ] Test rate limit: 101 requests/min → 429
- [ ] Forgot password envía email + token expira en 1h

**Estimación**: 3 días

---

### TASK 3.2 — Onboarding wizard (2 días)

Page `/app/(dashboard)/onboarding/page.tsx` mostrado primera vez (flag `users.onboarded BOOLEAN DEFAULT FALSE`).

5 pasos:
1. Datos despacho (nombre, NIF/CIF, dirección, teléfono, colegio + nº colegiado)
2. Logo upload (Supabase Storage)
3. Áreas principales (multi-select)
4. Invitar miembros equipo (opcional)
5. Plantilla honorarios por área (importar defaults o personalizar)

Submit final → `users.onboarded=true` → redirect `/dashboard`.

Si user no onboarded, layout `/app/(dashboard)/layout.tsx` redirige a `/onboarding`.

**Estimación**: 2 días

---

### TASK 3.3 — Tests E2E Playwright (2 días)

#### 3.3.1 Setup

```bash
cd /Users/nicolasag/CRM-LEGAL/apps/web
npm i -D @playwright/test
npx playwright install chromium
```

Config `/playwright.config.ts` apuntando a `http://localhost:3002`.

#### 3.3.2 Tests — `/apps/web/tests/e2e/`

```
auth.spec.ts       — login, logout, session persistence
dashboard.spec.ts  — KPIs cargan, links navegan
cases.spec.ts      — list + create + edit + delete + summary AI
documents.spec.ts  — upload + auto-analyze + classify + view detail
lexia.spec.ts      — chat envía + recibe stream + delete history
plazos.spec.ts     — calcula plazo + .ics descarga
docflow.spec.ts    — crea presupuesto + emite factura
```

Cada test usa storage state pre-autenticado (login una vez en globalSetup).

#### 3.3.3 GitHub Actions CI — `/.github/workflows/e2e.yml`

```yaml
name: E2E Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '24' }
      - run: cd apps/web && npm ci --legacy-peer-deps
      - run: cd apps/web && npx playwright install chromium
      - run: cd apps/web && npm run dev &
      - run: cd apps/web && npx wait-on http://localhost:3000
      - run: cd apps/web && npx playwright test
```

#### 3.3.4 Acceptance criteria

- [ ] 7 specs corren green local
- [ ] CI workflow corre y publica reports
- [ ] Tests determinísticos (no flaky)

**Estimación**: 2 días

---

### TASK 3.4 — Langfuse tracing activo (1 día)

Langfuse self-hosted en `localhost:3030` (Karen setup previo).

#### 3.4.1 SDK

```bash
cd apps/web && npm i langfuse
```

#### 3.4.2 Wrap Claude calls — `/lib/lexia/observability.ts`

```ts
import { Langfuse } from 'langfuse'

let _lf: Langfuse | null = null
export function lf() {
  if (!_lf && process.env.LANGFUSE_PUBLIC_KEY) {
    _lf = new Langfuse({
      publicKey: process.env.LANGFUSE_PUBLIC_KEY,
      secretKey: process.env.LANGFUSE_SECRET_KEY,
      baseUrl: process.env.LANGFUSE_HOST || 'http://localhost:3030',
    })
  }
  return _lf
}

export async function tracedClaudeCall(name, fn, metadata) {
  const trace = lf()?.trace({ name, metadata })
  const gen = trace?.generation({ name, input: metadata.input, model: metadata.model })
  try {
    const result = await fn()
    gen?.end({ output: result.output, usage: result.usage })
    return result
  } catch (err) {
    gen?.end({ level: 'ERROR', statusMessage: err.message })
    throw err
  }
}
```

#### 3.4.3 Wire en endpoints

- `/api/claude/chat/route.ts`: wrap `streamClaudeAPI` call
- `/api/skills/execute/route.ts`: wrap
- `/api/cases/[id]/summary/route.ts`: wrap
- `/api/documents/classify/route.ts`: wrap

Cada call envía: prompt, completion, latency, input/output tokens, cache hits, cost.

#### 3.4.4 Acceptance criteria

- [ ] Conversación con LEXIA aparece en Langfuse UI con tokens/costs
- [ ] Tool calls dentro de loop agéntico se trazan como child spans
- [ ] Latency P50/P95 visible por endpoint

**Estimación**: 1 día

---

### TASK 3.5 — Email outbound + Domain TLS (1 día)

#### 3.5.1 Resend

```bash
npm i resend
```

`/lib/email/send.ts`:
```ts
import { Resend } from 'resend'
const resend = new Resend(process.env.RESEND_API_KEY!)

export async function sendEmail(opts: {
  to: string; subject: string; html?: string; text?: string
  attachments?: { filename: string; content: Buffer }[]
}) {
  return resend.emails.send({
    from: 'IURALEX <hola@iuralex.cliender.com>',
    ...opts,
  })
}
```

Crear cuenta Resend + verificar dominio `iuralex.cliender.com` (DNS records).

Plantillas HTML `/lib/emails/templates/`:
- `deadline-alert.html` · `budget-sent.html` · `engagement-pending-sign.html` · `invoice-issued.html` · `welcome.html` · `password-reset.html`

#### 3.5.2 Domain prod

DNS apuntar `iuralex.cliender.com` a VPS Hetzner:
```
A     iuralex.cliender.com    → <VPS_IP>
TXT   _resend...              → DKIM verification
```

Caddy reverse proxy en VPS:
```
iuralex.cliender.com {
  reverse_proxy localhost:3002
  encode gzip
}
```

#### 3.5.3 Acceptance criteria

- [ ] `https://iuralex.cliender.com` carga con TLS válido
- [ ] Email test deadline alert llega a inbox real desde `hola@iuralex.cliender.com`
- [ ] DKIM/SPF/DMARC verificados

**Estimación**: 1 día

---

## 6. SPRINT 4 — P3 ESCALA

**Objetivo**: features para clientes grandes y diferenciación avanzada.
**Total**: 4-6 semanas.

### TASK 4.1 — Portal cliente externo (5 días)

Subdomain `cliente.iuralex.cliender.com` con magic link auth.

Schema:
```sql
CREATE TABLE client_portal_tokens (
  token TEXT PRIMARY KEY,
  client_id UUID NOT NULL REFERENCES clients(id),
  expires_at TIMESTAMPTZ NOT NULL,
  used_at TIMESTAMPTZ
);
```

UI cliente final (separada de IURALEX abogado):
- Login magic link
- Ver sus expedientes (read-only)
- Ver/firmar hojas de encargo
- Ver facturas
- Subir documentos (Supabase Storage)
- Mensajes con su abogado

### TASK 4.2 — Inbound triage automático (3 días)

Formulario público en web cliender → POST a `/api/inbound/lead`:
1. LEXIA clasifica área (Civil/Penal/Laboral/...)
2. Asigna abogado del equipo según specialty + carga
3. Crea ficha cliente + caso draft
4. Email confirmación al lead + Slack/email al abogado asignado

### TASK 4.3 — LexNET integración (7 días)

Requiere certificado digital ACA. SOAP API.

Endpoints LexNET:
- Notificaciones recibidas (poll diario)
- Presentación escritos
- Acuses de recibo

Complejo. Sólo si cliente lo pide explícitamente.

### TASK 4.4 — Facturae XML AEAT (3 días)

Generar XML Facturae 3.2.2 desde invoice + firmar con certificado. Envío FACe (puntos de entrada AAPP).

### TASK 4.5 — vLex / CENDOJ API real (5 días)

vLex tiene API privada (contactar BD). CENDOJ scraping con Firecrawl ya cubre.

### TASK 4.6 — Reseñas Google post-cierre (1 día)

Cuando `case.status → closed`:
1. Esperar 3 días (cool-down)
2. Email/WhatsApp al cliente con link directo a Google My Business reseña
3. Track open rate + conversion

---

## 7. REGLAS DE ORO PARA EJECUCIÓN

### Antes de cualquier tarea

1. **Lee este archivo entero** + el `CLAUDE.md` raíz del proyecto.
2. Verifica container `iuralex` Up: `docker ps | grep iuralex`.
3. Login funciona: `nicolas@cliender.com / Master123`.
4. Confirma `git status` limpio o haz commit antes.

### Durante implementación

5. **Una tarea = una rama git** si cambios grandes. Sprints completos = branch por sprint.
6. **Migrations son inmutables** una vez aplicadas — si te equivocas, crea migration nueva que revierte/corrige. NO edites una migration ya corrida.
7. **RLS first**: cada tabla nueva enable RLS + policy mínima desde el principio.
8. **Demo mode + prod mode**: cada endpoint que toque datos debe funcionar en ambos modos (`IS_DEV_WITHOUT_DB`).
9. **Tipografía + colores**: respetar convenciones sección 2. No introducir `bg-violet-600`, `bg-indigo-*`, `bg-purple-*` nuevos.
10. **Anthropic API key**: vive en `~/.env` como `ANTHROPIC_API_KEY`. Docker compose la inyecta. NO commitear.
11. **LEXIA system prompt**: NO modificar reglas 1-9. Si necesitas comportamiento nuevo, añade rule 10+ al final.
12. **Tests**: si añades feature de producto, añade spec E2E en mismo PR.

### Antes de marcar tarea completada

13. **HTTP 200 en endpoints nuevos**: verifica con `curl -b /tmp/c.txt ...`
14. **Container logs limpios**: `docker logs iuralex --tail 30` sin errors/exceptions
15. **HMR aplicó cambios**: re-test en browser después de save
16. **Demo data poblado**: si añades schema, añade entries demo coherentes
17. **Acceptance criteria**: marca cada checkbox real, no aspiracional

### Cuando algo falla

18. **Lee error completo** en `docker logs iuralex --tail 50`
19. **TS errors**: lee mensaje, no inventes types. `grep` el símbolo en codebase para uso real
20. **Anthropic 400**: probablemente system prompt mal formado (array vs string) — ver `lib/claude/client.ts:streamClaudeAPI`
21. **Supabase RLS denied**: el `(SELECT auth.uid())` pattern es obligatorio en policies — ver migration 004

---

## 8. GLOSARIO

### Archivos críticos

| Path | Propósito |
|------|-----------|
| `/lib/dev/demo-data.ts` | Toda la data demo (cases, clients, docs, analyses, contracts...) |
| `/lib/claude/prompts.ts` | SYSTEM_PROMPT_LEGAL_CHAT + sub-prompts contratos |
| `/lib/claude/client.ts` | `streamClaudeAPI`, `anthropic.client`, `MODEL` |
| `/lib/lexia/spanish-sources.ts` | 17 CCAA + BOE/CENDOJ/TC/AEPD source registry |
| `/lib/lexia/grounding.ts` | searchSpanishSources (mock + Firecrawl real path) |
| `/lib/legal-deadlines.ts` | 41 plazos + calculateDeadline + isSpanishHoliday |
| `/lib/security/apiResponse.ts` | withErrorHandler, UnauthorizedError, NotFoundError |
| `/lib/security/gdpr.ts` | deleteUserData, enforceDataRetention, auditLog |
| `/lib/hooks/useCachedFetch.ts` | Cache 30s + dedup in-flight |
| `/lib/auth/auth.ts` | Server auth check (JWT decode) |
| `/lib/auth/auth.config.ts` | NextAuth config |
| `/lib/supabase/server.ts` | Server client (service role) |
| `/lib/supabase/client.ts` | Browser client (anon) |
| `/app/(dashboard)/layout.tsx` | Sidebar + nav + logout |
| `/middleware.ts` | Auth check + rate limit (in-memory) + CSP headers |
| `/components/ui/*` | Badge, Avatar, AlertBox, TimelineEvent |
| `/components/features/equipo/` | Member detail components |
| `/components/features/command-palette/` | Cmd+K palette (a mejorar) |
| `/components/brand/ClienderLogo.tsx` | Logo C oficial SVG path |

### Env vars críticos

```
ANTHROPIC_API_KEY=sk-ant-api03-...
NEXTAUTH_SECRET=khl1a91WQ7C0MG5EtCoKP0kGBsIxIyqFsH0lIIL86TI=
NEXT_PUBLIC_SUPABASE_URL=https://icwsoxxdlctxcdkzcuny.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...
NEXT_PUBLIC_DEMO_MODE=1
FIRECRAWL_API_KEY=fc-bf8e536259314492a0806d506ac532ed
LANGFUSE_HOST=http://langfuse-web:3000
LANGFUSE_PUBLIC_KEY=pk-lf-c8d41e46fac40cbb0a10d9f465afc72d
LANGFUSE_SECRET_KEY=sk-lf-0fcc2175b48c6c27a13891b653d9ae2818e5a27997d9958f386d64daf4c5f4d9
```

Vars nuevas a añadir según sprint:
- `ENCRYPTION_KEY` (Sprint 1.3) — `openssl rand -hex 32`
- `SIGNATURIT_CLIENT_ID`, `SIGNATURIT_CLIENT_SECRET`, `SIGNATURIT_WEBHOOK_SECRET` (Sprint 1.3)
- `CRON_SECRET` (Sprint 1.2)
- `RESEND_API_KEY` (Sprint 3.5)
- `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET` (Sprint 3.1)
- `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN` (Sprint 3.1)

### Endpoints comunes

| Action | Endpoint |
|--------|----------|
| Login dev | `POST /api/auth/login` `{email, password}` |
| Get session | `GET /api/auth/session` |
| LEXIA chat stream | `POST /api/claude/chat` `{message, clientId?, conversation_id?}` |
| Doc classify | `POST /api/documents/classify` `{documentMarkdown}` |
| Case summary | `GET /api/cases/[id]/summary` (SSE) |
| Skills exec | `POST /api/skills/execute` `{skillId, documentMarkdown, caseId?}` |

### IDs demo importantes

```
USER:    00000000-0000-0000-0000-000000000001 (Nicolas)
CLIENT1: c1000000-0000-0000-0000-000000000001 (Construcciones Mediterráneo)
CLIENT2: c2000000-0000-0000-0000-000000000002 (María García Pérez)
CLIENT3: c3000000-0000-0000-0000-000000000003 (TechNova Innovations)
CASE1:   e1000000-0000-0000-0000-000000000001 (Vicios constructivos Vallecas)
CASE2:   e2000000-0000-0000-0000-000000000002 (Despido improcedente LogiSur)
CASE3:   e3000000-0000-0000-0000-000000000003 (NDA TechNova-DataCore)
DOC1:    d1000000-0000-0000-0000-000000000001 (Informe pericial Vallecas)
MEMBER1: m-1 (María González — Socio)
```

---

## ORDEN DE EJECUCIÓN RECOMENDADO

### Día 1-2: TASK 1.1 parte 1 (DocFlow schema + endpoints + demo data)
- Migration 005
- Endpoints budgets, engagements, invoices
- DEMO_FEE_SCHEDULES, DEMO_BUDGETS, DEMO_INVOICES

### Día 3: TASK 1.1 parte 2 (PDF rendering)
- Instalar @react-pdf/renderer
- BudgetPDF, EngagementPDF, InvoicePDF
- Endpoint pdf con render to buffer

### Día 4-5: TASK 1.1 parte 3 (UI + auto-flow)
- /finanzas page + tabs
- Forms + LineItemsEditor
- Hook auto-generate invoice on case close
- Botón "Generar presupuesto" en case detail

### Día 6: TASK 1.2 completa (cron plazos)
- Migration 006
- Endpoints deadlines + cron
- Dashboard widget + banner
- Email Resend

### Día 7: TASK 1.3 (Signaturit) + TASK 2.4 (Cmd+K)
- ENCRYPTION_KEY setup
- Migration 007
- OAuth Signaturit
- Mejorar command palette

### Día 8-10: TASK 2.1 (LEXIA agéntico)
- Tools definitions
- Tool executors
- Agentic loop
- UI tool feedback

### Día 11-12: TASK 2.2 (CaseHunter) + TASK 2.3 (grounding BOE)
- Migration 008
- Endpoint cron CaseHunter
- Firecrawl wire en search_jurisprudence
- /insights page

### Día 13-19: SPRINT 3
- 2FA + Google SSO + Redis (3d)
- Onboarding wizard (2d)
- Tests E2E (2d)

### Día 20-21: SPRINT 3 finalización
- Langfuse wire (1d)
- Domain + Resend (1d)

---

## CIERRE

Este plan está secuenciado por ROI. Sprint 1 cierra ventas. Sprint 2 da wow demos. Sprint 3 prod-ready. Sprint 4 escala.

**Si solo hubiera tiempo para 7 días → Sprint 1 entero + TASK 2.1 (LEXIA agéntico).** Cubre el 90% de objeciones reales de despachos.

**Total estimado**:
- Sprint 1: 6-7 días
- Sprint 2: 5-6 días
- Sprint 3: 6-7 días
- Sprint 4: 18-25 días

**Coste IA estimado** (sin Sprint 4): ~$150-300 en tokens Anthropic durante desarrollo. Producción 50 abogados activos: ~$200-500/mes Claude Haiku 4.5 con prompt caching.

Powered by Cliender Tech · 2026-05-22.
